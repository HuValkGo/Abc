@{
    Layout = "_Layout";
    ViewData["Title"] = Model.PageTitle;
    var itemId = Model.ItemId;
}
<p>
    <h1>@ViewData["Title"]</h1>
</p>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Details</h4>
    <hr />
    <dl class="row">
        @RenderBody()
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@itemId "></a>
    <a asp-page="./Index">Back to List</a>
</div>
